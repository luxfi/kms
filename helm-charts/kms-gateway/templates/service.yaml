apiVersion: v1
kind: Service
metadata:
  name: {{ include "kms-gateway.fullname" . }}
  labels:
    {{- include "kms-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kms-gateway.selectorLabels" . | nindent 4 }}
