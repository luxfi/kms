// Stub V1 EE routes - enterprise features removed
import { FastifyPluginAsync } from "fastify";

export const registerV1EERoutes: FastifyPluginAsync = async (server) => {
  // V1 Enterprise Edition routes would be registered here
  // This is a stub - enterprise features removed
};