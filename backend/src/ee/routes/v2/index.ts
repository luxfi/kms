// Stub V2 EE routes - enterprise features removed
import { FastifyPluginAsync } from "fastify";

export const registerV2EERoutes: FastifyPluginAsync = async (server) => {
  // V2 Enterprise Edition routes would be registered here
  // This is a stub - enterprise features removed
};