// Stub EST router - enterprise feature removed
import { FastifyPluginAsync } from "fastify";

export const registerCertificateEstRouter: FastifyPluginAsync = async (server) => {
  // EST (Enrollment over Secure Transport) endpoints would be here
  // This is a stub - enterprise features removed
};