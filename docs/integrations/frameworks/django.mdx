---
title: "Django"
description: "How to use KMS to inject environment variables and secrets into a Django app."
---

Prerequisites:

- Set up and add envars to [KMS Cloud](https://kms.lux.network)
- [Install the CLI](/cli/overview)

## Initialize KMS for your [Django](https://www.djangoproject.com) project

```bash
# navigate to the root of your of your project 
cd /path/to/project

# then initialize KMS
kms init
```

## Start your application as usual but with KMS

```bash
kms run -- <your application start command>

# Example
kms run -- python manage.py runserver
```
