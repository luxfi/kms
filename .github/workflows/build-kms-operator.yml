name: Build KMS Operator
on:
  push:
    branches: [main]
    paths:
      - 'k8-operator/**'
  workflow_dispatch:

jobs:
  docker:
    uses: hanzoai/.github/.github/workflows/docker-build.yml@main
    with:
      image: ghcr.io/luxfi/kms-operator
      context: k8-operator
    secrets: inherit
